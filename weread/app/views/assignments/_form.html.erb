<%= bootstrap_form_for(@assignment, :url => assignments_path, :html => { :multipart => true }, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-10" ) do |f| %>
	<p>
		<%= f.alert_message "Please fix the errors below.", class: "errormsg" %>
	</p>
	<p>
		<%= f.text_field :title, label: "* Title" %>
	</p>
	<p>
		<%= f.text_area :content, rows: "10" %>
	</p>
	<p>
		<%= f.datetime_select :duedate, ampm: true, label: "* Due Date" %>
	</p>
	<p>
		<%= f.file_field :attachment, class: "file", label: "Select File" %>
	</p>
	<p>
		<%= f.submit "Create", class: "btn btn-info" %>
		<%= link_to 'Cancel', assignments_path, class: 'btn btn-warning' %>
	</p>
<% end %>